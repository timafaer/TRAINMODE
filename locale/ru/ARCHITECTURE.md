# Русская локализация

`locale.cfg` содержит русский перевод сущностей, предметов, технологии и
настроек.

```text
ключ из prototype/settings
  -> соответствующая секция locale.cfg
  -> русская подпись в игре
```

Набор ключей должен совпадать с `locale/en/locale.cfg`.
