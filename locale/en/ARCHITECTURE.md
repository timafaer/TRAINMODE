# English Locale

`locale.cfg` содержит английские названия сущностей, предметов, технологии и
настроек.

```text
prototype name / setting name
  -> section key in locale.cfg
  -> English UI caption
```

Новые ключи сначала добавляются сюда, затем зеркально в `locale/ru/`.
