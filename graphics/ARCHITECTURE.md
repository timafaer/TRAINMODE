# Graphics

```text
исходный сгенерированный PNG
  -> удаление chroma-key
  -> icons/trainmode-universal.png
  -> resize 256x256
  -> icons/trainmode-universal-256.png
  -> prototypes/*
```

Сейчас все сущности используют единый тестовый значок. Замена визуала не должна
требовать изменений runtime-кода.
